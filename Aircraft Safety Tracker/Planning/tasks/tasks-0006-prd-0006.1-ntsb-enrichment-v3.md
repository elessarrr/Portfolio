## Relevant Files

- `Planning/tasks/0006-prd-0006.1-ntsb-enrichment-v3.md` - PRD reference (FR-1 through FR-9).
- `app/ingestion/importers/ntsb_importer.py` - NTSB parse contract; enrichment write mode (post-review gate).
- `app/ingestion/url_builders/ntsb.py` - `resolve_ntsb_source_url()` — single source of truth for Details URLs (FR-5.1).
- `app/ingestion/dedupe/ntsb_asn.py` - Score-based dedupe vs ASN (FR-4).
- `app/ingestion/importers/base.py` - `find_boeing_airbus_aircraft_id()` lookup-only; candidate home for `validate_ntsb_url()` (FR-5.2).
- `app/link_picker.py` - Returns NTSB `IncidentSource.source_url` when ASN absent (FR-8.2).
- `app/models.py` - `Aircraft`, `Incident`, `IncidentSource` schema.
- `scripts/audit_ntsb_enrichment.py` - Audit CLI: dedupe + link viability; extend for full corpus (FR-7).
- `scripts/export_ntsb_boeing_airbus.py` - (new) Export full Boeing/Airbus NTSB corpus from v2 `IncidentSource.source_data`.
- `data/raw/ntsb_records.json` - 500-record sample input (gitignored).
- `data/raw/ntsb_records_full.json` - (new) Full Boeing/Airbus NTSB corpus export (gitignored).
- `data/aircraft_safety.db` - v2 DB bootstrap source (~82k NTSB `IncidentSource` rows).
- `data/aircraft_safety_v3.db` - v3 ASN baseline target (0 NTSB rows today).
- `data/logs/ntsb_enrichment_audit.json` - Sample audit output (500 records; pre-link-check).
- `data/logs/ntsb_enrichment_full_audit.json` - (new) Full-corpus audit output (FR-7.2 buckets).
- `tests/test_ntsb_importer.py` - Existing NTSB parse + URL resolver tests.
- `tests/test_ntsb_dedupe.py` - (new) Dedupe scoring unit tests (FR-4.4, FR-9.1).
- `tests/test_ntsb_link_viability.py` - (new) Dead-end docket detection tests (FR-5.2, FR-9.1).

### Notes

- **PRD pipeline:** parse → dedupe vs ASN → viable unique set → resolve URL + viability check → audit report → **[REVIEW GATE]** → write-path import.
- **Boeing/Airbus only** (FR-1); no merge into ASN incidents — skip duplicates, insert unique only when link is viable.
- **UI freeze** (FR-8.3): No template/component changes until full-corpus link viability audit is approved.
- **Hard stop:** Stop after **Parent Task 5** (full-corpus viability audit) for product review before write-path import (Parent Task 6+).
- **Sample audit finding (2026-05-28):** All 10 `would_insert_sample` docket URLs opened to *“The docket for this investigation has not been released”* — mostly foreign-led (`*WA*`, `*RA*`, `cm_agency=Other`) and DirectorBrief records.
- **500-record sample counts (pre-link-check):** parsed 500, deduped 17, missing_aircraft 439, would_insert 44 — full corpus must **include** unknown-aircraft Boeing/Airbus rows (FR-6.1).
- Run tests from repo root: `PYTHONPATH=. pytest -q`.
- Audit against v3 DB: set `DATABASE_URL=sqlite:////absolute/path/to/data/aircraft_safety_v3.db` (or equivalent Flask config).
- Reuse v2 link-validation patterns from archived PRD-0019/0022 (`validate_source_url` docket body check for `"has not been released"`) — port into v3, do not depend on v2 CLI.

## Tasks

- [x] 1.0 Define the “NTSB-only enrichment” data contract + storage strategy (FR-8)
  - [x] 1.1 Decide where the NTSB Details URL lives: `IncidentSource.source_url` + `link_picker`; ASN incidents keep `Incident.asn_url` (FR-8.1–FR-8.2).
  - [x] 1.2 Define canonical NTSB input schema (JSON list matching `NTSBImporter.parse()` contract) (FR-2.1).
  - [x] 1.3 Define minimum `Incident` fields for NTSB-only rows: date, aircraft model, operator, location, fatalities, description, incident_type (FR-3.3).
  - [x] 1.4 Confirm `IncidentSource.source_data` stays metadata-only; no `links[]` blob (FR-8.2).
  - [x] 1.5 Document Boeing/Airbus aircraft resolution from NTSB make/model (`find_boeing_airbus_aircraft_id` lookup-only for audit) (FR-6).

- [x] 2.0 Implement NTSB acquisition path(s) for prototype import (FR-2)
  - [x] 2.1 Inventory v2 patterns: file-based import, logs, idempotency.
  - [x] 2.2 Add audit entrypoint `scripts/audit_ntsb_enrichment.py` with `--input` CLI flag (FR-2.2).
  - [x] 2.3 Manual file drop default path `data/raw/ntsb_records.json` (FR-2.3).
  - [x] 2.4 (Optional) Download+cache stub — deferred until live NTSB endpoint chosen (FR-2.3 open question).
  - [x] 2.5 Document acquisition options and prototype default (v2 export bootstrap) in PRD §7 (FR-2.3).

- [ ] 3.0 Build deterministic deduplication vs ASN + dedupe unit tests (FR-4)
  - [x] 3.1 Normalization helpers for operator + location (lowercase, punctuation trim, whitespace collapse).
  - [x] 3.2 Score-based dedupe function: date ±1 day, location fuzzy, operator fuzzy, fatalities proximity (FR-4.2).
  - [x] 3.3 ASN-covered threshold: ≥ 2 strong signals — codify as constants (FR-4.3).
  - [x] 3.4 Candidate ASN query: same `aircraft_id`, date window ±7 days, `asn_url` present (FR-4.1).
  - [x] 3.5 Audit report output: counts + `would_insert_sample` + closest ASN match for deduped rows (FR-4.5).
  - [ ] 3.6 Add `tests/test_ntsb_dedupe.py` covering FR-4.4 edge cases:
    - [ ] 3.6.1 Same date + operator/location close ⇒ ASN-covered (skip).
    - [ ] 3.6.2 Date ±1 day + one strong signal only ⇒ not covered (would insert).
    - [ ] 3.6.3 Clearly different incident ⇒ not covered.
    - [ ] 3.6.4 Deterministic: same inputs always same decision.

- [ ] 4.0 Implement link viability gate + refresh 500-record sample audit (FR-5)
  - [ ] 4.1 Implement `validate_ntsb_url(url) -> (viable: bool, status_code, reason)` in v3 (port docket GET + body check from archived PRD-0019): reject `"has not been released"` (FR-5.2).
  - [ ] 4.2 Document additional dead-end patterns as discovered (DirectorBrief docket, `cm_agency=Other` fallback) in function docstring or module comment (FR-5.4).
  - [ ] 4.3 Wire link check into `scripts/audit_ntsb_enrichment.py` after URL resolution via `resolve_ntsb_source_url()` (FR-5.1, FR-5.3).
  - [ ] 4.4 Extend audit report buckets: `viable_with_working_link`, `viable_with_broken_link`, `skipped_unreleased_docket` (FR-5.2 subset for sample run).
  - [ ] 4.5 Add `tests/test_ntsb_link_viability.py` with mocked HTTP: 200 + unreleased body ⇒ not viable; 200 + content ⇒ viable (FR-9.1).
  - [ ] 4.6 Re-run 500-record sample audit; write updated `data/logs/ntsb_enrichment_audit.json`; note working vs broken link counts for product.

- [ ] 5.0 Full-corpus NTSB viability audit — dedupe all + link check viable set (FR-6, FR-7)
  - [ ] 5.1 Add `scripts/export_ntsb_boeing_airbus.py`: export all Boeing/Airbus NTSB records from v2 `data/aircraft_safety.db` `IncidentSource.source_data` → `data/raw/ntsb_records_full.json` (FR-7.1 bootstrap).
  - [ ] 5.2 Update audit script: **include** Boeing/Airbus records when `aircraft_id` not found in v3 DB — do not increment `skipped_missing_aircraft`; track `unknown_aircraft_id` separately (FR-6.1).
  - [ ] 5.3 Run dedupe on **full** corpus against v3 ASN baseline; remove ASN duplicates → `viable_unique` set (FR-7.2).
  - [ ] 5.4 For unknown-aircraft rows, document limited dedupe scope in report (FR-6.2).
  - [ ] 5.5 Run link viability check on `viable_unique` set with polite sequential requests (rate-limit aware; cache results to avoid re-fetch) (FR-5.2, open question #4).
  - [ ] 5.6 Write `data/logs/ntsb_enrichment_full_audit.json` with FR-7.2 buckets:
    - `total_parsed`, `skipped_not_boeing_airbus`, `skipped_deduped_asn_covered`, `viable_unique`, `viable_with_working_link`, `viable_with_broken_link`
    - Include ≥10 sample rows per bucket (FR-7.3).
  - [ ] 5.7 **REVIEW GATE (hard stop):** Product reviews full audit — viable unique count + working-link count — before any write-path import (FR-7.4).

- [ ] 6.0 Adapt NTSB importer write path — insert only viable unique incidents (FR-3; after review gate)
  - [ ] 6.1 Refactor `NTSBImporter` enrichment mode: create `Incident` + `IncidentSource` only when dedupe pass **and** link viability pass (FR-3.1–FR-3.4).
  - [ ] 6.2 Idempotency: upsert on `IncidentSource(source_name, source_record_id)`; re-run same input does not duplicate (FR-3).
  - [ ] 6.3 Wire Details link: `Incident.asn_url` null for NTSB-only; `IncidentSource.source_url` via resolver; `link_picker.pick_primary_href` returns NTSB URL (FR-8.2).
  - [ ] 6.4 Preserve foreign-led/DirectorBrief resolver rules; never insert rows whose URL failed viability check (FR-5.4).
  - [ ] 6.5 Recalc `Aircraft` stats (`total_incidents`, `fatal_incidents`, `total_fatalities`) after import.

- [ ] 7.0 Test + QA — post-write verification (FR-9)
  - [ ] 7.1 Extend importer unit tests: enrichment mode insert vs skip; idempotent re-run (FR-9.1).
  - [ ] 7.2 Run `PYTHONPATH=. pytest -q`; suite green (FR-9.2).
  - [ ] 7.3 Manual localhost smoke against v3 DB (FR-9.3):
    - [ ] 7.3.1 Run enrichment import on approved viable set only.
    - [ ] 7.3.2 Confirm NTSB-only rows look identical to ASN rows (no UI/template changes).
    - [ ] 7.3.3 Click Details on NTSB-only incidents; confirm working CAROL or released docket — no “docket not released”.
