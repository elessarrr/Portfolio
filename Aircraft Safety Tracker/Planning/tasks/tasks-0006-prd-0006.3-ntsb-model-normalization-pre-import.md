## Relevant Files

- `Planning/tasks/0006-prd-0006.3-ntsb-model-normalization-pre-import.md` - PRD reference (FR-13–FR-22; inherits FR-1–FR-12 from 0006.1/0006.2).
- `Planning/tasks/0006-prd-0006.2-ntsb-enrichment-v3-audit-export.md` - Prior PRD (Tasks 5.7–5.11 complete).
- `Planning/tasks/0006-prd-0006.1-ntsb-enrichment-v3.md` - Parent PRD (Tasks 1.0–5.6 complete).
- `Planning/tasks/tasks-0006-prd-0006.2-ntsb-enrichment-v3-audit-export.md` - Prior task list (superseded by this file for Tasks 5.12+).
- `Planning/reviews/ntsb-family-rollup-proposal.md` - **(new)** Product review doc for family rollup (FR-15).
- `app/ingestion/importers/base.py` - `find_boeing_airbus_aircraft_id()` (lookup-only); add mapping-aware resolver; restrict auto-create (FR-17).
- `app/ingestion/importers/ntsb_importer.py` - Accept mapping file; fail closed on unmapped strings (FR-17, FR-21).
- `app/ingestion/ntsb_mapping.py` - Load/validate mapping; `bootstrap_create_approved_pages()` (FR-16, FR-17, FR-20.0).
- `app/ingestion/dedupe/ntsb_asn.py` - Score-based dedupe vs ASN; reused for dedupe re-pass (FR-18).
- `app/ingestion/url_builders/ntsb.py` - `resolve_ntsb_source_url_checked()` — unchanged from 0006.2.
- `app/ingestion/url_builders/ntsb_viability.py` - `validate_ntsb_url()` — unchanged from 0006.2.
- `app/ingestion/audit_export.py` - JSONL export helpers; extend for normalized export (FR-19).
- `app/models.py` - `Aircraft.model_name` String(64); length audit only unless migration needed (FR-14).
- `app/link_picker.py` - Returns NTSB `IncidentSource.source_url` when ASN absent (FR-8.2).
- `scripts/export_ntsb_make_model_catalog.py` - **(new)** Distinct make_model catalog from working-link export (FR-13).
- `scripts/audit_ntsb_enrichment.py` - Full-corpus audit CLI (dedupe re-pass via dedicated script).
- `scripts/ntsb_dedupe_repass.py` - **(new)** Dedupe re-pass CLI on 657 working rows with mapping gate (FR-18).
- `scripts/ntsb_pilot_import.py` - Pilot workflow: clone → select → bootstrap → import → verify (FR-20).
- `scripts/ntsb_bulk_import.py` - **(new)** Bulk import on real v3: bootstrap → dedupe → import → stats (FR-21).
- `data/logs/ntsb_bulk_import_report.json` - **(new)** Bulk import report + idempotent verify.
- `data/logs/ntsb_bootstrap_create_approved.json` - **(new)** Bootstrap report (created vs already existed).
- `app/ingestion/ntsb_dedupe_repass.py` - **(new)** Core dedupe re-pass logic + normalized export writer (FR-18, FR-19).
- `Planning/reviews/ntsb-pre-import-review-gate-2026-05-30.md` - **(new)** Pre-import review gate doc (FR-19.3).
- `app/ingestion/ntsb_post_import_audit.py` - **(new)** Post-import audit + remediation (FR-22).
- `scripts/export_ntsb_boeing_airbus.py` - Export full Boeing/Airbus NTSB corpus from v2 DB.
- `data/config/ntsb_make_model_to_aircraft.jsonl` - **(new, committed)** Approved pre-import mapping gate (FR-16).
- `data/logs/ntsb_enrichment_audit_rows.jsonl` - 657 working-link rows + full audit export (0006.2).
- `data/logs/ntsb_make_model_catalog.jsonl` - **(new)** Distinct make_model catalog (FR-13).
- `data/logs/ntsb_model_name_length_audit.json` - **(new)** Length audit result (FR-14).
- `data/logs/ntsb_dedupe_repasse.json` - **(new)** Dedupe re-pass summary (FR-18).
- `data/logs/ntsb_enrichment_audit_rows_normalized.jsonl` - **(new)** Working rows + mapping + dedupe status (FR-19).
- `data/logs/ntsb_pre_import_summary.json` - **(new)** Pre-import counts for review gate (FR-19).
- `data/logs/ntsb_post_import_audit.json` - **(new)** Post-import duplicate audit (FR-22).
- `data/aircraft_safety_v3.db` - Real v3 ASN baseline (bulk import target).
- `data/aircraft_safety_v3.db.pre-ntsb-bootstrap` - **(new, gitignored)** Point-in-time backup before real-v3 bootstrap/bulk import (FR-20.0.6; 2026-05-30).
- `data/aircraft_safety_v3_pilot.db` - **(new, gitignored)** Throwaway clone for pilot import (FR-20).
- `data/raw/ntsb_records_full.json` - Full corpus input (3,710 records; gitignored).
- `tests/test_ntsb_dedupe.py` - Dedupe scoring unit tests.
- `tests/test_ntsb_link_viability.py` - Link viability unit tests.
- `tests/test_ntsb_audit_export.py` - Export row schema + bucket count integrity.
- `tests/test_ntsb_importer.py` - NTSB parse + URL resolver tests; extend for mapping gate.
- `tests/test_ntsb_mapping.py` - **(new)** Mapping loader + resolver unit tests (FR-16, FR-17).
- `tests/test_ntsb_dedupe_repasse.py` - **(new)** Dedupe re-pass with mapped aircraft_id (FR-18).

### Notes

- **0006.3 scope:** Tasks **5.12–5.22** (normalization + pre-import gate) then **6.0–7.0** (import + QA). Tasks **1.0–5.11** inherited complete from 0006.1/0006.2.
- **Decisions (PRD §9):** variant threshold **≥ 10**; **one** Stearman page; **new** helicopter rows; mapping file **committed**; pilot on **throwaway DB clone**; real v3 **file backup** before bulk (`aircraft_safety_v3.db.pre-ntsb-bootstrap`).
- **Audit state (2026-05-31):** **657** working links; **632** `unknown_aircraft=true`; **279** distinct `make_model` strings; max string length **45** chars (fits 64).
- **Pipeline:** catalog → length audit → rollup proposal → mapping JSONL → import gate → dedupe re-pass → pre-import review → **clone pilot DB → bootstrap catalog pages (FR-20.0)** → pilot incident import → **backup real v3 DB (FR-20.0.6)** → bootstrap on real v3 → dedupe re-pass → bulk import → post-import audit.
- **Review workflow:** `grep -v '^#' data/logs/ntsb_enrichment_audit_rows.jsonl | jq 'select(.bucket=="viable_with_working_link")'`
- **Task 6.0 blocked** until **5.19** (pre-import review gate) passes; bulk import blocked until **5.20** (pilot) passes.
- Run tests: `PYTHONPATH=. pytest -q` from repo root.
- Audit/dedupe re-pass: `DATABASE_URL=sqlite:////absolute/path/to/data/aircraft_safety_v3.db`
- Bootstrap catalog pages: `DATABASE_URL=sqlite:////absolute/path/to/data/aircraft_safety_v3_pilot.db PYTHONPATH=. python scripts/bootstrap_ntsb_create_approved_pages.py`
- **Backup real v3 before bulk (FR-20.0.6):** `cp data/aircraft_safety_v3.db data/aircraft_safety_v3.db.pre-ntsb-bootstrap` — verified 2026-05-30 (97 aircraft, 5,523 incidents, 0 NTSB). Rollback: swap files or point `DATABASE_URL` at backup.

## Tasks

- [x] 1.0 Define the “NTSB-only enrichment” data contract + storage strategy (FR-8) — *0006.1 complete*
  - [x] 1.1 NTSB Details URL: `IncidentSource.source_url` + `link_picker`; ASN uses `Incident.asn_url` (FR-8.1–FR-8.2).
  - [x] 1.2 Canonical NTSB input schema (`NTSBImporter.parse()` contract) (FR-2.1).
  - [x] 1.3 Minimum `Incident` fields for NTSB-only rows (FR-3.3).
  - [x] 1.4 `IncidentSource.source_data` metadata-only; no `links[]` blob (FR-8.2).
  - [x] 1.5 Boeing/Airbus aircraft resolution (`find_boeing_airbus_aircraft_id` lookup-only) (FR-6).

- [x] 2.0 Implement NTSB acquisition path(s) for prototype import (FR-2) — *0006.1 complete*
  - [x] 2.1 Inventory v2 patterns: file-based import, logs, idempotency.
  - [x] 2.2 Audit entrypoint `scripts/audit_ntsb_enrichment.py` with `--input` (FR-2.2).
  - [x] 2.3 Manual file drop default `data/raw/ntsb_records.json` (FR-2.3).
  - [x] 2.4 (Optional) Download+cache stub — deferred (FR-2.3).
  - [x] 2.5 Document acquisition options in PRD §7 (FR-2.3).

- [x] 3.0 Build deterministic deduplication vs ASN + dedupe unit tests (FR-4) — *0006.1 complete*
  - [x] 3.1 Normalization helpers for operator + location.
  - [x] 3.2 Score-based dedupe function (FR-4.2).
  - [x] 3.3 ASN-covered threshold: ≥ 2 strong signals (FR-4.3).
  - [x] 3.4 Candidate ASN query: ±7 day window per `aircraft_id` (FR-4.1).
  - [x] 3.5 Audit report: counts + samples + closest ASN match (FR-4.5).
  - [x] 3.6 `tests/test_ntsb_dedupe.py` (FR-4.4):
    - [x] 3.6.1 Same date + operator/location close ⇒ ASN-covered.
    - [x] 3.6.2 Date ±1 day + one strong signal only ⇒ not covered.
    - [x] 3.6.3 Clearly different incident ⇒ not covered.
    - [x] 3.6.4 Deterministic: same inputs → same decision.

- [x] 4.0 Implement link viability gate + 500-record sample audit (FR-5) — *0006.1 complete*
  - [x] 4.1 `validate_ntsb_url()` in `ntsb_viability.py` (FR-5.2).
  - [x] 4.2 Document dead-end patterns (DirectorBrief, `cm_agency=Other`) (FR-5.4).
  - [x] 4.3 Wire link check into audit script after `resolve_ntsb_source_url()` (FR-5.1, FR-5.3).
  - [x] 4.4 Audit buckets: `viable_with_working_link`, `viable_with_broken_link`, `skipped_unreleased_docket`.
  - [x] 4.5 `tests/test_ntsb_link_viability.py` with mocked HTTP (FR-9.1).
  - [x] 4.6 Sample audit → `data/logs/ntsb_enrichment_audit.json` (44 viable; 2 working / 42 broken).

- [x] 5.0 Full-corpus audit + full row export + link review (FR-6, FR-7, FR-10, FR-11, FR-12) — *0006.2 complete through 5.11*
  - [x] 5.1 `scripts/export_ntsb_boeing_airbus.py` → `data/raw/ntsb_records_full.json` (3,710 records) (FR-7.1).
  - [x] 5.2 Include unknown-aircraft Boeing/Airbus rows; track `unknown_aircraft_included` separately (FR-6.1).
  - [x] 5.3 Full dedupe vs v3 ASN → `viable_unique` (61 deduped, 3,649 viable) (FR-7.2).
  - [x] 5.4 Document limited dedupe for unknown-aircraft rows in report (FR-6.2). **3,482 unknown.**
  - [x] 5.5 Link viability on viable set; sequential requests + in-run URL cache (FR-5.2). **~35–40 min full re-audit.**
  - [x] 5.6 Summary report → `data/logs/ntsb_enrichment_full_audit.json` (counts + ≥10 samples/bucket) (FR-7.3).
  - [x] 5.7 Add `--export-rows PATH` to audit CLI; stream every classified row to one JSONL file (FR-10.1–FR-10.4):
    - [x] 5.7.1 CLI flag `--export-rows` with default `data/logs/ntsb_enrichment_audit_rows.jsonl` (FR-10.7).
    - [x] 5.7.2 Each line: `bucket`, `source_record_id`, `date`, `make_model`, `operator`, `location`, `fatalities`, `ntsb_url`, `aircraft_id`, `unknown_aircraft`, `link_viable`, `link_reason`, `closest_asn_match` (FR-10.3).
    - [x] 5.7.3 Bucket values: `skipped_deduped_asn_covered`, `viable_with_working_link`, `viable_with_broken_link` (FR-10.3).
    - [x] 5.7.4 Stream writes (append per row); do not buffer full corpus in memory (FR-10.2).
  - [x] 5.8 Validate export integrity + unit tests (FR-10.6):
    - [x] 5.8.1 After audit, count lines per `bucket` in JSONL; compare to summary JSON counts.
    - [x] 5.8.2 Add `tests/test_ntsb_audit_export.py`: mock audit run writes expected buckets; counts match summary.
    - [x] 5.8.3 Run `PYTHONPATH=. pytest -q`; suite green.
  - [x] 5.9 Re-run full-corpus audit with export (FR-10.8). **3710 total lines; export_validation matched.**
  - [x] 5.10 **REVIEW GATE:** Product reviewed full export — links correct; make/model from bulk metadata (FR-11). **657 working-link rows approved for normalization path.**
  - [x] 5.11 **FR-12 re-audit:** CAROL empty-SPA + docket fallback; working links **679 → 657**; **0 CAROL** in working bucket.

- [x] 5.12 Export NTSB make_model catalog from 657 working rows (FR-13)
  - [x] 5.12.1 `scripts/export_ntsb_rollup_mapping_draft.py` (rollup + string catalog fields) (FR-13.1).
  - [x] 5.12.2 Filter `bucket=viable_with_working_link`; skip `#` comment lines in JSONL (FR-13.1).
  - [x] 5.12.3 **279** distinct strings in `data/logs/ntsb_rollup_string_mapping_draft.jsonl` with `incident_count`, `category`, proposed page (FR-13.2).
  - [x] 5.12.4 Sorted by `incident_count` descending in rollup draft export (FR-13.3–FR-13.4).
  - [x] 5.12.5 Sanity check: Stearman cluster, 737, 757, A320 at top of volume list.

- [x] 5.13 Run `model_name` length audit (FR-14)
  - [x] 5.13.1 Max NTSB string **45 chars** — under 64 limit (FR-14.1).
  - [x] 5.13.2 `data/logs/ntsb_model_name_length_audit.json` written (FR-14.4).
  - [x] 5.13.3 No `app/models.py` migration required (FR-14.2).

- [x] 5.14 Draft family rollup proposal (FR-15)
  - [x] 5.14.1 `Planning/reviews/ntsb-family-rollup-proposal.md` — Part A + appendix (FR-15.1).
  - [x] 5.14.2 `data/logs/ntsb_rollup_string_mapping_draft.jsonl` + `ntsb_rollup_incident_assignments_draft.jsonl` (FR-15.1).
  - [x] 5.14.3 Default rules applied: ≥10 → keep_variant; suffix rollups; casing dedupe (FR-15.2).
  - [x] 5.14.4 Stearman → one page; helicopters → `create_approved` with Boeing/Airbus in name (FR-15.2, FR-23).
  - [x] 5.14.5 **50** canonical targets (15 `create_approved` + 35 existing) — within ~15–30 family target range (FR-15.3).

- [x] 5.15 **REVIEW GATE:** Product approves rollup proposal (FR-15.4)
  - [x] 5.15.1 Part A reviewed — all 279 strings mapped (0 TBD); edge cases F4-622R, BOEING MD, BOEING CV2 confirmed.
  - [x] 5.15.2 Sign off on Stearman = one page; helicopters = new rows.

- [x] 5.16 Build approved canonical mapping JSONL (FR-16)
  - [x] 5.16.1 `data/config/ntsb_make_model_to_aircraft.jsonl` with version header (FR-16.5).
  - [x] 5.16.2 FR-16.2 schema: `ntsb_make_model`, `canonical_aircraft_id`, `canonical_model_name`, `action`, `manufacturer`/`notes`.
  - [x] 5.16.3 **100% coverage** — 279/279 working-bucket strings validated (FR-16.3).
  - [x] 5.16.4 Builder: `scripts/build_ntsb_make_model_mapping.py` (FR-16.4 many-to-one preserved).
  - [ ] 5.16.5 Commit mapping file to repo (PRD §9 — pending git commit).

- [x] 5.17 Implement mapping gate in importer; fail closed on unmapped (FR-17)
  - [x] 5.17.1 `app/ingestion/ntsb_mapping.py`: load JSONL, validate schema, resolve aircraft_id (FR-16, FR-17.2).
  - [x] 5.17.2 `resolve_aircraft_id_from_ntsb_mapping()` — lookup by canonical_model_name; `create_approved` idempotent (FR-17.1–FR-17.4).
  - [x] 5.17.3 `NTSBImporter(mapping=...)` skips unmapped → `skipped_unmapped` / `skipped_unresolved` lists (FR-17.3).
  - [x] 5.17.4 Stores `ntsb_make_model` on `IncidentSource.source_data` at write time.
  - [x] 5.17.5 `tests/test_ntsb_mapping.py` (6 tests); legacy auto-create when mapping omitted.
  - [x] 5.17.6 `PYTHONPATH=. pytest -q` — 52 passed.

- [x] 5.18 Dedupe re-pass + normalized pre-import summary (FR-18, FR-19)
  - [x] 5.18.1 `scripts/ntsb_dedupe_repass.py` re-runs dedupe on 657 working rows using **mapped** `aircraft_id` (lookup-only for `create_approved`) (FR-18.1).
  - [x] 5.18.2 `data/logs/ntsb_dedupe_repasse.json`: `newly_deduped_count`=51, `still_viable_count`=396, 10 samples (FR-18.2).
  - [x] 5.18.3 `data/logs/ntsb_enrichment_audit_rows_normalized.jsonl` — 657 rows with mapping + `dedupe_repasse_status` (FR-19.1).
  - [x] 5.18.4 `data/logs/ntsb_pre_import_summary.json`: 657 working, 396 import candidates, 0 unmapped, 51 ASN-covered, 35 distinct IDs (FR-19.2).
  - [x] 5.18.5 `tests/test_ntsb_dedupe_repasse.py` (3 tests); mapped aircraft_id enables dedupe audit skipped (FR-18.4).
  - [x] 5.18.6 `PYTHONPATH=. pytest -q` — 55 passed.

- [x] 5.19 **REVIEW GATE:** Product approves pre-import summary (FR-19.3)
  - [x] 5.19.1 Review `ntsb_pre_import_summary.json` counts vs expectations — see `Planning/reviews/ntsb-pre-import-review-gate-2026-05-30.md`.
  - [x] 5.19.2 Spot-check normalized JSONL: mapped aircraft pages match intent (samples in review doc).
  - [x] 5.19.3 Approve proceed to pilot import — signed off 2026-05-30 (chat).

- [x] 5.20 Pilot import — clone DB, bootstrap catalog pages, 30 incident rows (FR-20.0, FR-20)
  - [x] 5.20.0 Implement FR-20.0 bootstrap tooling
    - [x] 5.20.0.1 `bootstrap_create_approved_pages()` + FR-23 name validation in `app/ingestion/ntsb_mapping.py`.
    - [x] 5.20.0.2 `scripts/bootstrap_ntsb_create_approved_pages.py` with `--dry-run` and report JSON.
    - [x] 5.20.0.3 Bootstrap tests in `tests/test_ntsb_mapping.py` (8 mapping tests total).
    - [x] 5.20.0.4 Dry-run against mapping file: **15** distinct targets.
  - [x] 5.20.1 Copy `data/aircraft_safety_v3.db` → `data/aircraft_safety_v3_pilot.db`; add pilot DB to `.gitignore` (FR-20.4).
  - [x] 5.20.2 Bootstrap 15 `create_approved` catalog pages on **pilot DB** (FR-20.0); report at `data/logs/ntsb_bootstrap_create_approved_pilot.json`.
  - [x] 5.20.3 Re-run dedupe re-pass on pilot DB — 606 import candidates (was 396 pre-bootstrap); 51 ASN-covered unchanged (FR-20.0.5).
  - [x] 5.20.4 Select 25 rows with original `unknown_aircraft=false` + 5 high-confidence mapped rows → `data/logs/ntsb_pilot_import_manifest.jsonl` (FR-20.1).
  - [x] 5.20.5 Run incident import on pilot DB with mapping gate — **30/30 written** via `scripts/ntsb_pilot_import.py` (FR-20.2).
  - [x] 5.20.6 Verify: correct aircraft pages, Details links viable, no ASN duplicates — **0 issues** (FR-20.3).
  - [x] 5.20.7 Product sign-off; pilot DB retained at `data/aircraft_safety_v3_pilot.db` for debugging (FR-20.4).

- [x] 5.21 Bulk import — Task 6.0 write path on real v3 DB (FR-21)
  - [x] 5.21.0 **Backup real v3 DB** before bootstrap/bulk import (FR-20.0.6): `data/aircraft_safety_v3.db.pre-ntsb-bootstrap` — 97 aircraft, 5,523 incidents, **0** NTSB `IncidentSource` rows (2026-05-30). `.gitignore` includes `data/aircraft_safety_v3.db.*`.
  - [x] 5.21.1 Bootstrap `create_approved` pages on real v3 DB (FR-20.0); report at `data/logs/ntsb_bootstrap_create_approved.json` — **15 created**.
  - [x] 5.21.2 Re-run dedupe re-pass on real v3 DB after bootstrap — **606** import candidates (`ntsb_pre_import_summary.json`).
  - [x] 5.21.3 Import all `import_candidates_after_dedupe_repasse` rows — **606/606 written** via `scripts/ntsb_bulk_import.py` (FR-21.1).
  - [x] 5.21.4 Idempotent re-run verified — 0 new `IncidentSource` / `Incident` rows on second pass (FR-21.2).
  - [x] 5.21.5 Raw NTSB `make_model` preserved in `IncidentSource.source_data.ntsb_make_model` (FR-21.3).
  - [x] 5.21.6 Recalc `Aircraft` stats — **43** NTSB-touched pages updated (`total_incidents`, `fatal_incidents`, `total_fatalities`).

- [x] 5.22 Post-import duplicate audit (FR-22)
  - [x] 5.22.1 `scripts/audit_post_ntsb_import.py` + `app/ingestion/ntsb_post_import_audit.py` (FR-22.1).
  - [x] 5.22.2 Incident dupes, aircraft near-dupes, orphan URL checks (FR-22.1).
  - [x] 5.22.3 `data/logs/ntsb_post_import_audit.json` — **passed** after remediation (3 dupes removed).
  - [x] 5.22.4 Remediated 3 NTSB rows (`ATL02LA075`, `FTW96LA269`, `SEA02FA060`) — null→0 fatalities at import caused dedupe re-pass miss; **603** NTSB sources remain.

- [x] 6.0 Adapt NTSB importer write path — mapping-gated insert (FR-3, FR-17, FR-21; shipped via 5.21 + 6.8)
  - [x] 6.1 `NTSBImporter` enrichment mode: insert only when dedupe re-pass pass **and** link viability pass **and** mapping resolves (FR-3.1–FR-3.4, FR-17). Pipeline: normalized `dedupe_repasse_status=import` + working-link audit + `NTSBImporter(mapping=…)`.
  - [x] 6.2 Idempotency via `IncidentSource(source_name, source_record_id)` upsert (FR-3). Verified 5.21.4.
  - [x] 6.3 Details link: `IncidentSource.source_url` + `link_picker` when ASN absent (FR-8.2).
  - [x] 6.4 Never insert rows whose URL failed viability check (FR-5.4). Enforced at audit (`viable_with_working_link`); import uses that subset.
  - [x] 6.5 Never insert rows with unmapped `make_model` (FR-17.3). `NTSBImporter._resolve_aircraft_id` skips when mapping missing.
  - [x] 6.6 Import from normalized export subset (`dedupe_repasse_status=import`) — not raw 657 without re-pass (FR-21.1). `ntsb_bulk_import.import_candidates`.
  - [x] 6.7 `--mapping` required for bulk import CLI; document in PRD/README. Default gate path on `import` / `run-all`; help text documents FR-17.
  - [x] 6.8 Add **Make/Model** column to `incident_list.html`; NTSB rows show exact string from source metadata (FR-21.4). `display_make_model` + column in `incident_list.html`.

- [x] 7.0 Test + QA — post-write verification (FR-9, FR-22)
  - [x] 7.1 Importer unit tests: mapping gate insert vs skip; idempotent re-run (FR-9.1). Covered in `tests/test_ntsb_mapping.py` (`test_importer_mapping_gate_*`, `test_importer_idempotent_re_run_with_mapping`) + `tests/test_ntsb_bulk_import.py`.
  - [x] 7.2 Run `PYTHONPATH=. pytest -q` (FR-9.2). **71 passed** (2026-06-01).
  - [x] 7.3 Manual localhost smoke (FR-9.3) — Flask on `:5003` with `DATABASE_URL=…/aircraft_safety_v3.db`:
    - [x] 7.3.1 Spot-check across Stearman (`/aircraft/68`), A320 (`78`), AS350 (`99`), 737-800 (`23`); 12 NTSB Details hrefs fetched (200, no unreleased body).
    - [x] 7.3.2 NTSB rows share incident table layout with Make/Model column populated (FR-21.4).
    - [x] 7.3.3 Make/Model shows exact NTSB strings (e.g. `Boeing A75N1 (PT17)`, `Airbus Industrie A320`); page titles are rolled-up catalog names.
    - [x] 7.3.4 Details links viable — sampled docket URLs return 200 without “has not been released”.
    - [x] 7.3.5 Search `?q=Boeing` returns Boeing aircraft pages (FR-23).
